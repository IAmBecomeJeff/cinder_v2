TODO:

Review palette options
Investigate FastLED.addLeds<stuff>(stuff).setDither(BRIGHTNESS < 255);
Set up rotary dial to change lots more variables
Set up Serial input for keyboard testing
Experiment with circnoise values

